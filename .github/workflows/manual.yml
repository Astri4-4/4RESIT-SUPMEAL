name: Deploy to homelab

on:
  push:
    branches: [master]        # adjust if your default branch is actually "main"
  workflow_dispatch:          # adds the manual "Run workflow" button
    inputs:
      tag:
        description: 'Image tag to deploy'
        required: false
        default: 'latest'
        type: string

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/supmeal:${{ inputs.tag || 'latest' }}

  deploy:
    needs: build-and-push
    runs-on: [self-hosted, homelab]
    steps:
      - name: Redeploy
        run: |
          cd /opt/supmeal
          docker compose pull
          docker compose up -d
